import type { EmploymentTableRow } from './AboutTableSchema'

// the data that is linked to the work is from the projects file and is not kept here
export const EmployeeTableData: Array<EmploymentTableRow> = []
